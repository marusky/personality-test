- if !user_authenticated?
  = form_with url: login_path, method: :post do |f|
    = f.label :nickname
    = f.text_field :nickname
    = f.submit 'pozriet vysledok'
    - if @nickname_not_found
      Vysledky pre takyto nick sme nenasli.

  = link_to 'Namiesto toho urobit test', root_path
